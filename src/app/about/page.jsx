import AboutContes from '@/components/AboutContes';
import React from 'react';

const page = () => {
    return (
        <div>
            <AboutContes />
        </div>
    );
};

export default page;